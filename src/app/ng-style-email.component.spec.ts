/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NgStyleEmailComponent } from './ng-style-email.component';

describe('NgStyleEmailComponent', () => {
  let component: NgStyleEmailComponent;
  let fixture: ComponentFixture<NgStyleEmailComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ NgStyleEmailComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(NgStyleEmailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
